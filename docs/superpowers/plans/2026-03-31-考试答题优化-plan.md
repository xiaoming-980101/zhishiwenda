# 考试答题优化实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复考试答题顺序、补充考试模式对多选/填空/场景题的支持、完善题库原句上下文

**Architecture:** 最小改动方案：修改组卷逻辑保持题型顺序，在考试模式中补充缺失题型的渲染和交互，逐题修复省略的原句

**Tech Stack:** Vue 3、TypeScript、Vant UI

---

## 文件结构

| 文件 | 改动类型 | 职责 |
|------|----------|------|
| `src/views/mock-exam/index.vue` | 修改 | 组卷逻辑：取消整体打乱 |
| `src/views/practice/index.vue` | 修改 | 考试模式：补充多选/填空/场景题支持 |
| `src/data/exam.ts` | 修改 | 数据修复：补充原句上下文 |

---

### Task 1: 修改组卷逻辑，保持题型顺序

**Files:**
- Modify: `src/views/mock-exam/index.vue:62`

- [ ] **Step 1: 修改 buildExamPaperByConfig 函数**

将第62行的 `return shuffleArray(selectedQuestions)` 改为 `return selectedQuestions`：

```javascript
// 改动前（第62行）
return shuffleArray(selectedQuestions)

// 改动后
return selectedQuestions  // 不再打乱整体顺序，按配置顺序返回
```

- [ ] **Step 2: 验证修改**

启动开发服务器，进入模拟考试页面，确认题目顺序为：单选题 → 判断题 → 填空题 → 场景题。

- [ ] **Step 3: 提交**

```bash
git add src/views/mock-exam/index.vue
git commit -m "fix: 考试答题按题型顺序出题，不再打乱整体顺序"
```

---

### Task 2: 扩展考试答案类型定义

**Files:**
- Modify: `src/views/practice/index.vue:54-66, 90`

- [ ] **Step 1: 修改 mockExamStorage 类型定义**

修改第54-66行的类型定义：

```typescript
// 改动前
const mockExamStorage = useLocalStorage('cardwinner-mock-exam-state', {
  // ...
  objectiveAnswers: {} as Record<number, string | boolean>,
  // ...
})

// 改动后
const mockExamStorage = useLocalStorage('cardwinner-mock-exam-state', {
  // ...
  objectiveAnswers: {} as Record<number, string | boolean | string[] | Record<number, any>>,
  // ...
})
```

- [ ] **Step 2: 修改 examObjectiveAnswers 响应式变量**

修改第90行：

```typescript
// 改动前
const examObjectiveAnswers = reactive<Record<number, string | boolean>>({ ...safeMockExamStorage.value.objectiveAnswers })

// 改动后
const examObjectiveAnswers = reactive<Record<number, string | boolean | string[] | Record<number, any>>>({ ...safeMockExamStorage.value.objectiveAnswers })
```

- [ ] **Step 3: 提交**

```bash
git add src/views/practice/index.vue
git commit -m "refactor: 扩展考试答案类型支持多选/填空/场景题"
```

---

### Task 3: 添加考试模式多选题支持

**Files:**
- Modify: `src/views/practice/index.vue` (script区域添加函数，template区域修改)

- [ ] **Step 1: 添加多选题交互函数**

在 script 区域的 `answerExamObjective` 函数后面（约第308行）添加：

```typescript
function toggleExamMultipleAnswer(questionId: number, label: string) {
  if (submitted.value) return
  const current = examObjectiveAnswers[questionId] as string[] | undefined
  if (!current) {
    examObjectiveAnswers[questionId] = [label]
  } else {
    const index = current.indexOf(label)
    if (index > -1) {
      current.splice(index, 1)
      if (current.length === 0) {
        delete examObjectiveAnswers[questionId]
      }
    } else {
      current.push(label)
      current.sort()
    }
  }
  unlockNextQuestion()
}

function isExamMultipleSelected(questionId: number, label: string): boolean {
  const current = examObjectiveAnswers[questionId] as string[] | undefined
  return current ? current.includes(label) : false
}
```

- [ ] **Step 2: 添加多选题答案判断函数**

在 `isExamCorrect` 函数后面添加：

```typescript
function isExamMultipleCorrect(question: PracticeQuestion): boolean {
  const userAnswer = examObjectiveAnswers[question.id] as string[] | undefined
  if (!userAnswer) return false
  const correctAnswer = question.answer as string[]
  return correctAnswer.length === userAnswer.length && correctAnswer.every(a => userAnswer.includes(a))
}
```

- [ ] **Step 3: 修改考试答题区域模板**

找到约第1277-1314行的考试答题模板，将现有的单选/判断逻辑改为支持多选题：

```vue
<div class="mt-[14px] space-y-[10px]">
  <!-- 单选题 -->
  <template v-if="currentExamQuestion.type === 'single'">
    <button
      v-for="option in getOrderedOptions(currentExamQuestion)"
      :key="option.label"
      type="button"
      class="answer-card"
      :disabled="submitted"
      :class="examOptionClass(currentExamQuestion, option.label)"
      @click="answerExamObjective(option.label)"
    >
      <span class="answer-label">{{ option.label }}</span>
      <span class="flex-1 text-left">{{ option.text }}</span>
    </button>
  </template>

  <!-- 多选题 -->
  <template v-else-if="currentExamQuestion.type === 'multiple'">
    <button
      v-for="option in currentExamQuestion.options"
      :key="option.label"
      type="button"
      class="answer-card"
      :disabled="submitted"
      :class="{
        selected: isExamMultipleSelected(currentExamQuestion.id, option.label),
        correct: submitted && (currentExamQuestion.answer as string[]).includes(option.label),
        wrong: submitted && isExamMultipleSelected(currentExamQuestion.id, option.label) && !(currentExamQuestion.answer as string[]).includes(option.label),
      }"
      @click="toggleExamMultipleAnswer(currentExamQuestion.id, option.label)"
    >
      <span class="answer-label">{{ option.label }}</span>
      <span class="flex-1 text-left">{{ option.text }}</span>
    </button>
    <div v-if="!submitted" class="mt-[8px] text-[12px] text-[var(--van-text-color-2)]">
      多选题：可选择多个答案，点击已选项可取消
    </div>
  </template>

  <!-- 判断题 -->
  <template v-else-if="currentExamQuestion.type === 'judge'">
    <button
      type="button"
      class="answer-card"
      :disabled="submitted"
      :class="examOptionClass(currentExamQuestion, true)"
      @click="answerExamObjective(true)"
    >
      <span class="answer-label">A</span>
      <span class="flex-1 text-left">正确</span>
    </button>
    <button
      type="button"
      class="answer-card"
      :disabled="submitted"
      :class="examOptionClass(currentExamQuestion, false)"
      @click="answerExamObjective(false)"
    >
      <span class="answer-label">B</span>
      <span class="flex-1 text-left">错误</span>
    </button>
  </template>
</div>
```

- [ ] **Step 4: 修改答案解析区域**

在提交后的答案解析区域（约第1315-1330行），添加多选题的答案显示：

```vue
<div v-if="submitted && examObjectiveAnswers[currentExamQuestion.id] !== undefined" class="analysis-box mt-[14px]">
  <!-- 单选/判断题答案 -->
  <div v-if="currentExamQuestion.type === 'single' || currentExamQuestion.type === 'judge'">
    <div class="font-bold">
      正确答案：{{ currentExamQuestion.answer === true ? '正确' : currentExamQuestion.answer === false ? '错误' : currentExamQuestion.answer }}
    </div>
  </div>

  <!-- 多选题答案 -->
  <div v-else-if="currentExamQuestion.type === 'multiple'">
    <div class="font-bold">
      正确答案：{{ (currentExamQuestion.answer as string[]).join(', ') }}
    </div>
    <div class="mt-[4px] text-[14px]">
      你的答案：{{ (examObjectiveAnswers[currentExamQuestion.id] as string[])?.join(', ') || '未作答' }}
    </div>
  </div>

  <div class="mt-[8px] text-[14px] leading-[24px]">
    {{ currentExamQuestion.explanation }}
  </div>
  <div class="proof-box mt-[10px]">
    <div class="font-bold">原句依据</div>
    <div class="mt-[4px] text-[14px] leading-[24px]">
      {{ currentExamQuestion.proof }}
    </div>
  </div>
</div>
```

- [ ] **Step 5: 提交**

```bash
git add src/views/practice/index.vue
git commit -m "feat: 考试模式支持多选题答题"
```

---

### Task 4: 添加考试模式填空题支持

**Files:**
- Modify: `src/views/practice/index.vue`

- [ ] **Step 1: 添加填空题交互函数**

在 `isExamMultipleCorrect` 函数后面添加：

```typescript
function setExamFillBlankAnswer(questionId: number, slotId: number, value: string) {
  if (submitted.value) return
  const current = examObjectiveAnswers[questionId] as Record<number, string> | undefined
  if (!current) {
    examObjectiveAnswers[questionId] = { [slotId]: value }
  } else {
    current[slotId] = value
  }
}

function getExamFillBlankAnswer(questionId: number, slotId: number): string {
  const current = examObjectiveAnswers[questionId] as Record<number, string> | undefined
  return current ? current[slotId] || '' : ''
}

function isExamFillBlankCorrect(question: PracticeQuestion): boolean {
  const slots = question.fillSlots || []
  const userAnswer = examObjectiveAnswers[question.id] as Record<number, string> | undefined
  if (!userAnswer) return false
  return slots.every(slot => {
    const userValue = userAnswer[slot.slotId] || ''
    return slot.keywords.some(keyword => userValue.toLowerCase().includes(keyword.toLowerCase()))
  })
}
```

- [ ] **Step 2: 在答题模板中添加填空题渲染**

在 Task 3 的模板中，判断题模板后面添加填空题模板：

```vue
  <!-- 填空题 -->
  <template v-else-if="currentExamQuestion.type === 'fillBlank'">
    <div
      v-for="slot in currentExamQuestion.fillSlots"
      :key="slot.slotId"
      class="fill-blank-item"
    >
      <div class="text-[14px] font-bold text-[var(--color-brand-deep)] mb-[6px]">
        第 {{ slot.slotId }} 空
      </div>
      <van-field
        :model-value="getExamFillBlankAnswer(currentExamQuestion.id, slot.slotId)"
        :placeholder="slot.placeholder"
        :disabled="submitted"
        clearable
        @update:model-value="(val: string) => setExamFillBlankAnswer(currentExamQuestion.id, slot.slotId, val)"
      />
    </div>
    <div v-if="!submitted" class="mt-[8px] text-[12px] text-[var(--van-text-color-2)]">
      填空题：答案包含关键词即可得分
    </div>
  </template>
</div>
```

- [ ] **Step 3: 在答案解析中添加填空题显示**

在答案解析区域的 `<!-- 多选题答案 -->` 后面添加：

```vue
  <!-- 填空题答案 -->
  <div v-else-if="currentExamQuestion.type === 'fillBlank'">
    <div class="font-bold">正确答案：</div>
    <div class="mt-[4px] space-y-[4px]">
      <div v-for="slot in currentExamQuestion.fillSlots" :key="slot.slotId">
        第 {{ slot.slotId }} 空：{{ slot.keywords.join(' / ') }}
      </div>
    </div>
    <div class="mt-[8px] font-bold">你的答案：</div>
    <div class="mt-[4px] space-y-[4px]">
      <div v-for="slot in currentExamQuestion.fillSlots" :key="slot.slotId">
        第 {{ slot.slotId }} 空：{{ getExamFillBlankAnswer(currentExamQuestion.id, slot.slotId) || '未填写' }}
      </div>
    </div>
  </div>
```

- [ ] **Step 4: 提交**

```bash
git add src/views/practice/index.vue
git commit -m "feat: 考试模式支持填空题答题"
```

---

### Task 5: 添加考试模式场景题支持

**Files:**
- Modify: `src/views/practice/index.vue`

- [ ] **Step 1: 添加场景题交互函数**

在 `isExamFillBlankCorrect` 函数后面添加：

```typescript
function setExamScenarioAnswer(questionId: number, subId: number, value: string | boolean | string[] | Record<number, string>) {
  if (submitted.value) return
  const current = examObjectiveAnswers[questionId] as Record<number, any> | undefined
  if (!current) {
    examObjectiveAnswers[questionId] = { [subId]: value }
  } else {
    current[subId] = value
  }
}

function getExamScenarioAnswer(questionId: number, subId: number): string | boolean | string[] | Record<number, string> | undefined {
  const current = examObjectiveAnswers[questionId] as Record<number, any> | undefined
  return current ? current[subId] : undefined
}

function toggleExamScenarioMultipleAnswer(questionId: number, subId: number, label: string) {
  if (submitted.value) return
  const current = getExamScenarioAnswer(questionId, subId) as string[] | undefined
  if (!current) {
    setExamScenarioAnswer(questionId, subId, [label])
  } else {
    const index = current.indexOf(label)
    if (index > -1) {
      current.splice(index, 1)
      if (current.length === 0) {
        const parent = examObjectiveAnswers[questionId] as Record<number, any>
        delete parent[subId]
      }
    } else {
      current.push(label)
      current.sort()
    }
  }
}

function isExamScenarioMultipleSelected(questionId: number, subId: number, label: string): boolean {
  const current = getExamScenarioAnswer(questionId, subId) as string[] | undefined
  return current ? current.includes(label) : false
}

function setExamScenarioFillBlankAnswer(questionId: number, subId: number, slotId: number, value: string) {
  if (submitted.value) return
  const current = getExamScenarioAnswer(questionId, subId) as Record<number, string> | undefined
  if (!current) {
    setExamScenarioAnswer(questionId, subId, { [slotId]: value })
  } else {
    current[slotId] = value
  }
}

function getExamScenarioFillBlankAnswer(questionId: number, subId: number, slotId: number): string {
  const current = getExamScenarioAnswer(questionId, subId) as Record<number, string> | undefined
  return current ? current[slotId] || '' : ''
}
```

- [ ] **Step 2: 在答题模板中添加场景题渲染**

在填空题模板后面添加场景题模板：

```vue
  <!-- 场景题 -->
  <template v-else-if="currentExamQuestion.type === 'scenario'">
    <!-- 场景描述 -->
    <div v-if="currentExamQuestion.scenarioDesc" class="scenario-desc-box mt-[12px]">
      <div class="font-bold text-[var(--color-brand-deep)]">场景描述</div>
      <div class="mt-[8px] text-[15px] leading-[24px]">
        {{ currentExamQuestion.scenarioDesc }}
      </div>
    </div>

    <!-- 子问题 -->
    <div
      v-for="subQ in currentExamQuestion.subQuestions"
      :key="subQ.subId"
      class="sub-question-box mt-[14px]"
    >
      <div class="flex items-center justify-between mb-[8px]">
        <div class="text-[14px] font-bold text-[var(--color-brand-deep)]">
          问题 {{ subQ.subId }}（{{ subQ.type === 'judge' ? '判断' : subQ.type === 'multiple' ? '多选' : '填空' }}）
        </div>
        <span class="sub-score-tag">{{ subQ.score }} 分</span>
      </div>
      <div class="text-[16px] leading-[24px]">{{ subQ.stem }}</div>

      <!-- 子问题-判断 -->
      <div v-if="subQ.type === 'judge'" class="mt-[10px] space-y-[8px]">
        <button
          type="button"
          class="answer-card small"
          :disabled="submitted"
          :class="{ selected: getExamScenarioAnswer(currentExamQuestion.id, subQ.subId) === true }"
          @click="setExamScenarioAnswer(currentExamQuestion.id, subQ.subId, true)"
        >
          <span class="answer-label small">A</span>
          <span class="flex-1 text-left">正确</span>
        </button>
        <button
          type="button"
          class="answer-card small"
          :disabled="submitted"
          :class="{ selected: getExamScenarioAnswer(currentExamQuestion.id, subQ.subId) === false }"
          @click="setExamScenarioAnswer(currentExamQuestion.id, subQ.subId, false)"
        >
          <span class="answer-label small">B</span>
          <span class="flex-1 text-left">错误</span>
        </button>
      </div>

      <!-- 子问题-多选 -->
      <div v-else-if="subQ.type === 'multiple'" class="mt-[10px] space-y-[8px]">
        <button
          v-for="option in subQ.options"
          :key="option.label"
          type="button"
          class="answer-card small"
          :disabled="submitted"
          :class="{ selected: isExamScenarioMultipleSelected(currentExamQuestion.id, subQ.subId, option.label) }"
          @click="toggleExamScenarioMultipleAnswer(currentExamQuestion.id, subQ.subId, option.label)"
        >
          <span class="answer-label small">{{ option.label }}</span>
          <span class="flex-1 text-left">{{ option.text }}</span>
        </button>
      </div>

      <!-- 子问题-填空 -->
      <div v-else-if="subQ.type === 'fillBlank'" class="mt-[10px] space-y-[8px]">
        <div v-for="slot in subQ.fillSlots" :key="slot.slotId">
          <div class="text-[12px] text-[var(--van-text-color-2)] mb-[4px]">
            第 {{ slot.slotId }} 空
          </div>
          <van-field
            :model-value="getExamScenarioFillBlankAnswer(currentExamQuestion.id, subQ.subId, slot.slotId)"
            :placeholder="slot.placeholder"
            :disabled="submitted"
            clearable
            @update:model-value="(val: string) => setExamScenarioFillBlankAnswer(currentExamQuestion.id, subQ.subId, slot.slotId, val)"
          />
        </div>
      </div>
    </div>
  </template>
</div>
```

- [ ] **Step 3: 在答案解析中添加场景题显示**

在填空题答案解析后面添加场景题答案解析：

```vue
  <!-- 场景题答案 -->
  <div v-else-if="currentExamQuestion.type === 'scenario'">
    <div class="font-bold">子问题答案：</div>
    <div class="mt-[8px] space-y-[12px]">
      <div
        v-for="subQ in currentExamQuestion.subQuestions"
        :key="subQ.subId"
        class="sub-answer-box"
      >
        <div class="text-[14px] font-bold text-[var(--color-brand-deep)]">
          问题 {{ subQ.subId }}（{{ subQ.type === 'judge' ? '判断' : subQ.type === 'multiple' ? '多选' : '填空' }}）
        </div>
        <div v-if="subQ.type === 'judge'" class="mt-[6px]">
          <div class="text-[14px]">
            <span class="text-[var(--van-text-color-2)]">正确答案：</span>
            <span class="font-bold">{{ subQ.answer === true ? '正确' : '错误' }}</span>
          </div>
        </div>
        <div v-else-if="subQ.type === 'multiple'" class="mt-[6px]">
          <div class="text-[14px]">
            <span class="text-[var(--van-text-color-2)]">正确答案：</span>
            <span class="font-bold">{{ (subQ.answer as string[]).join('、') }}</span>
          </div>
        </div>
        <div v-else-if="subQ.type === 'fillBlank'" class="mt-[6px] space-y-[4px]">
          <div v-for="slot in subQ.fillSlots" :key="slot.slotId" class="text-[14px]">
            第 {{ slot.slotId }} 空关键词：{{ slot.keywords.join(' / ') }}
          </div>
        </div>
      </div>
    </div>
  </div>
```

- [ ] **Step 4: 提交**

```bash
git add src/views/practice/index.vue
git commit -m "feat: 考试模式支持场景题答题"
```

---

### Task 6: 修复得分明细显示

**Files:**
- Modify: `src/views/practice/index.vue`

- [ ] **Step 1: 修改 isExamAnswered 函数支持新题型**

找到 `isExamAnswered` 函数（约第257-259行），修改为支持所有题型：

```typescript
function isExamAnswered(question: PracticeQuestion) {
  const answer = examObjectiveAnswers[question.id]
  if (answer === undefined) return false
  if (question.type === 'fillBlank') {
    const slots = question.fillSlots || []
    const fillAnswer = answer as Record<number, string>
    return slots.some(slot => fillAnswer[slot.slotId])
  }
  if (question.type === 'scenario') {
    const subQs = question.subQuestions || []
    const scenarioAnswer = answer as Record<number, any>
    return subQs.some(subQ => scenarioAnswer[subQ.subId] !== undefined)
  }
  return true
}
```

- [ ] **Step 2: 修改 isExamCorrect 函数支持新题型**

找到 `isExamCorrect` 函数（约第261-265行），修改为：

```typescript
function isExamCorrect(question: PracticeQuestion) {
  if (!isExamAnswered(question)) return false
  const answer = examObjectiveAnswers[question.id]

  if (question.type === 'multiple') {
    return isExamMultipleCorrect(question)
  }
  if (question.type === 'fillBlank') {
    return isExamFillBlankCorrect(question)
  }
  if (question.type === 'scenario') {
    const subQs = question.subQuestions || []
    const scenarioAnswer = answer as Record<number, any>
    return subQs.every(subQ => {
      const subAnswer = scenarioAnswer[subQ.subId]
      if (subAnswer === undefined) return false
      if (subQ.type === 'multiple') {
        const correctAnswer = subQ.answer as string[]
        const userAnswerArr = subAnswer as string[]
        return correctAnswer.length === userAnswerArr.length && correctAnswer.every(a => userAnswerArr.includes(a))
      }
      if (subQ.type === 'fillBlank') {
        const slots = subQ.fillSlots || []
        const fillAnswer = subAnswer as Record<number, string>
        return slots.every(slot => {
          const userValue = fillAnswer[slot.slotId] || ''
          return slot.keywords.some(keyword => userValue.toLowerCase().includes(keyword.toLowerCase()))
        })
      }
      return subAnswer === subQ.answer
    })
  }
  return answer === question.answer
}
```

- [ ] **Step 3: 提交**

```bash
git add src/views/practice/index.vue
git commit -m "fix: 修复考试模式得分明细对新题型的支持"
```

---

### Task 7: 修复题库原句上下文

**Files:**
- Modify: `src/data/exam.ts`

- [ ] **Step 1: 修复表格引用类原句**

搜索 `文档三.1表格`、`文档三.2表格`、`文档4.2表格` 等表格引用，替换为完整表述：

```typescript
// 题目401
proof: '物销易合同类型建议为实物营销，适用场景是实物营销活动设计，是通用型产品可在卡控或星辰平台使用。',

// 题目402
proof: '好车服合同类型建议为车生活服务合同，承接洗车、代驾、车险咨询等车生活服务，是卡控专属产品。',

// 题目406
proof: '智企推合同类型建议为营销推广合同，承接银行分期推广、媒介投放、保险精准营销等推广类业务，是卡控专属产品。',
```

- [ ] **Step 2: 修复章节引用类原句**

搜索 `文档4.2：`、`文档三.1：`、`文档三.2：` 等章节引用，补充完整上下文：

```typescript
// 题目403
proof: '路径A通过星辰/赢商城平台进行活动设计后发货，收入归实际使用的平台（星辰或赢商城）。',

// 题目404
proof: '路径B直接通过卡控平台分发，使用智捷通、数益达、物销易等通用型产品，收入归卡控平台。',

// 题目407
proof: '慧商通B2B业务模式同时承担两类业务：正常的B2B销售和外部直发业务记录，统一归属赢商城平台。',
```

- [ ] **Step 3: 逐题检查所有 proof 字段**

遍历 `practiceQuestions` 数组，检查每个题目的 `proof` 字段，确保：
- 无"文档X.X表格"等省略引用
- 无"原文"等模糊表述
- 包含足够上下文让人理解

- [ ] **Step 4: 提交**

```bash
git add src/data/exam.ts
git commit -m "fix: 补充题库原句上下文，移除省略引用"
```

---

## 验证清单

- [ ] 考试答题顺序：单选 → 判断 → 填空 → 场景，每次重新组卷保持此顺序
- [ ] 考试答题支持：单选、多选、判断、填空、场景题均可正常答题
- [ ] 答案解析：所有题型提交后显示正确答案和解析
- [ ] 原句完整性：无"文档三.1表格"等省略引用，上下文完整